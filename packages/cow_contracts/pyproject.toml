[project]
name = "cow-contracts"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "web3>=7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cow_contracts"]
